meta {
  name: JoinGame
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/joinGame/{{gameId}}
  body: json
  auth: none
}

body:json {
  {
    "playerName": "Player2"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("myPlayerId", res.body.playerId);
  }
}
